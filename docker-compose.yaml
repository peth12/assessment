version: '3.8'

services:
#  app:
#    image: 'docker-spring-boot-postgres:latest'
#    build:
#      context: .
#    container_name: app
#    depends_on:
#      - db
#    environment:
#      - POSTGRES_USER=username
#      - POSTGRES_PASSWORD=password
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5435/lottery
#      - SPRING_DATASOURCE_USERNAME=username
#      - SPRING_DATASOURCE_PASSWORD=password
#      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
#
#    ports:
#      - "8080:8080"

  db:
    image: postgres
    container_name: db
    environment:
      - POSTGRES_USER=username
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=lottery
    ports:
      - "5435:5432"